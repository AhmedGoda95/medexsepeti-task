import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#28527A",
      contrastText: "#fff",
    },
    secondary: {
      main: "#f9ba32",
    },
    divider: "#F9BA32",
  },
});

export default theme;
